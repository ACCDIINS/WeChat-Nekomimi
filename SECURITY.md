# 安全与隐私

## API Key 怎么填

在 `config.json` 的 `ai.api_key` 填写，或设置环境变量 `NEKOMIMI_API_KEY`（优先于配置文件）。

Key 只保存在本机，程序不会上传到除你所选 API 服务商以外的服务器。

## 数据会发到哪里

| 场景 | 行为 |
|------|------|
| 短句（规则猫化） | 完全本地处理 |
| 长句 / 抽样走 AI | 仅将当前待发送的单条文本发至你配置的 API |
| 聊天记录 | 不收集、不存储 |

具体数据处理方式以 API 服务商的用户协议为准。

## 程序权限

本工具使用全局键盘钩子读取微信输入框（通过剪贴板），以实现发送前改写。请在可信任的个人电脑上使用；若钩子无效，请以管理员身份运行 `run.bat`。

## Key 泄露怎么办

若怀疑 Key 已泄露，请立即在对应平台吊销并重新生成。
